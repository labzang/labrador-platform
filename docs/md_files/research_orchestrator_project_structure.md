# 연구 오케스트레이터 프로젝트 구조 제안

## 🎯 결론: 별도 프로젝트로 분리 권장

제시된 MCP 에이전트 오케스트레이터 코드는 현재 RAG 프로젝트와는 **완전히 다른 목적**을 가지므로 별도 프로젝트로 만드는 것이 최적입니다.

## 📊 비교 분석

| 항목 | 현재 RAG 프로젝트 | 제시된 오케스트레이터 |
|------|------------------|---------------------|
| **목적** | 스팸 이메일 탐지 | 연구 보고서 작성 |
| **도메인** | 이메일 분류 | 웹 검색 + 문서 작성 |
| **아키텍처** | 선형 파이프라인 | 다중 에이전트 협업 |
| **모델** | KoELECTRA + EXAONE | Anthropic Claude |
| **데이터** | 이메일 텍스트 | 웹 콘텐츠 + 파일 |

## 🏗️ 새 프로젝트 구조 제안

```
research-orchestrator/
├── README.md
├── requirements.txt
├── .env.example
├── task.md                    # 연구 태스크 정의
├── main.py                    # 제시된 코드 위치
├── output/                    # 결과 저장
│   ├── sources/              # 수집된 소스
│   └── execution_plan.md     # 실행 계획
├── config/
│   └── mcp_config.py         # MCP 설정
├── agents/
│   ├── __init__.py
│   ├── searcher.py           # 검색 에이전트
│   ├── fact_checker.py       # 팩트 체커
│   └── report_writer.py      # 보고서 작성자
├── workflows/
│   ├── __init__.py
│   └── research_workflow.py  # 연구 워크플로우
└── utils/
    ├── __init__.py
    └── file_manager.py       # 파일 관리
```

## 🔄 현재 프로젝트에 통합하지 않는 이유

### 1. **목적의 차이**
- **현재**: 실시간 스팸 탐지 서비스
- **제시된 코드**: 배치 형태의 연구 보고서 생성

### 2. **아키텍처 충돌**
- **현재**: FastAPI 웹 서비스 + 실시간 처리
- **제시된 코드**: 스크립트 기반 + 장시간 배치 처리

### 3. **의존성 충돌**
- **현재**: 한국어 모델 + 로컬 LLM
- **제시된 코드**: Anthropic Claude + 웹 검색 API

### 4. **사용자 경험**
- **현재**: API 엔드포인트 + 즉시 응답
- **제시된 코드**: CLI 스크립트 + 장시간 실행

## 🚀 구현 방안

### Option 1: 완전 별도 프로젝트 (권장) ✅
```bash
# 새 프로젝트 생성
mkdir research-orchestrator
cd research-orchestrator

# 제시된 코드를 main.py로 저장
# 필요한 의존성 설치
pip install mcp-agent anthropic rich
```

### Option 2: 현재 프로젝트에 선택적 통합 (비권장) ❌
```
app/
├── research/              # 새 모듈 추가
│   ├── __init__.py
│   ├── orchestrator.py    # 제시된 코드
│   └── agents/
└── routers/
    └── research_router.py # 새 라우터
```

**비권장 이유**:
- 프로젝트 복잡도 급증
- 의존성 충돌 위험
- 단일 책임 원칙 위반

## 💡 현재 프로젝트에서 활용할 수 있는 아이디어

제시된 코드에서 현재 프로젝트에 적용할 수 있는 패턴:

### 1. **다중 에이전트 패턴**
```python
# app/services/verdict_agent/multi_agent.py
class MultiAgentOrchestrator:
    def __init__(self):
        self.spam_agent = SpamClassifier()
        self.sentiment_agent = SentimentAnalyzer()
        self.content_agent = ContentAnalyzer()

    async def analyze_parallel(self, email):
        results = await asyncio.gather(
            self.spam_agent.analyze(email),
            self.sentiment_agent.analyze(email),
            self.content_agent.analyze(email)
        )
        return self.combine_results(results)
```

### 2. **계획 기반 실행**
```python
# app/controllers/planning_controller.py
class EmailAnalysisPlan:
    def create_plan(self, email_complexity):
        if email_complexity == "simple":
            return ["koelectra_only"]
        elif email_complexity == "medium":
            return ["koelectra", "quick_verdict"]
        else:
            return ["koelectra", "detailed_analysis", "fact_check"]
```

## 🎯 최종 권장사항

1. **별도 프로젝트로 생성** ✅
   - 목적과 아키텍처가 완전히 다름
   - 독립적인 개발과 배포 가능
   - 각각의 도메인에 최적화

2. **현재 프로젝트는 스팸 탐지에 집중** ✅
   - 기존 KoELECTRA + EXAONE 구조 유지
   - 하이브리드 라우팅으로 성능 최적화
   - 단일 도메인 전문성 강화

3. **필요시 마이크로서비스로 연결** 🔄
   - 두 시스템을 API로 연결
   - 각각 독립적으로 스케일링
   - 서로 다른 배포 주기 유지

**결론**: 제시된 코드는 새로운 `research-orchestrator` 프로젝트로 만드세요! 🚀
